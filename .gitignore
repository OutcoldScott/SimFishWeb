# Rust
target/
**/*.rs.bk

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
.env
.venv
venv/
ENV/

# Godot - editor cache + headless capture outputs (regenerated on demand).
shaders-godot/godot-project/.godot/
shaders-godot/godot-project/capture_raw.png
shaders-godot/godot-project/capture_quantized.png

# Web fallback fonts - downloaded + subsetted by the container font-builder
# stage at build time, never committed. The .import sidecars Godot generates
# for them are transient too.
shaders-godot/godot-project/fonts/

# Built macOS app + release artifacts - distributed via GitHub Releases.
build/

# OS generated files
.DS_Store
.DS_Store?
._*
.claude
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
